version: "3.8"

services:
  db:
    image: postgres:16-alpine
    container_name: polyflip_db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-polyflip}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
      POSTGRES_DB: ${DB_NAME:-polyflip}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - polyflip_db_data:/var/lib/postgresql/data
    deploy:
      resources:
        limits:
          memory: 512M

  api:
    build: .
    container_name: polyflip_api
    restart: always
    depends_on:
      - db
    ports:
      - "8001:8001"
    environment:
      - DATABASE_URL=postgresql+asyncpg://${DB_USER:-polyflip}:${DB_PASSWORD:-secret}@db/${DB_NAME:-polyflip}
      - API_KEY=${API_KEY:-test-key}
      - ASSETS=${ASSETS:-BTC,ETH,SOL,XRP,DOGE}
      - EXECUTION_MODE=${EXECUTION_MODE:-PAPER}
    volumes:
      - ./polyflip:/app/polyflip
      - ./scripts:/app/scripts
      - ./alembic:/app/alembic
      - ./alembic.ini:/app/alembic.ini
      - ./tests:/app/tests
    command: uvicorn polyflip.api.main:app --host 0.0.0.0 --port 8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/dashboard"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 512M

  scheduler:
    build: .
    container_name: polyflip_scheduler
    restart: always
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgresql+asyncpg://${DB_USER:-polyflip}:${DB_PASSWORD:-secret}@db/${DB_NAME:-polyflip}
      - API_KEY=${API_KEY:-test-key}
      - ASSETS=${ASSETS:-BTC,ETH,SOL,XRP,DOGE}
      - EXECUTION_MODE=${EXECUTION_MODE:-PAPER}
    volumes:
      - ./polyflip:/app/polyflip
      - ./scripts:/app/scripts
      - ./alembic:/app/alembic
      - ./alembic.ini:/app/alembic.ini
      - ./backups:/app/backups
    command: python -m polyflip.scheduler.jobs
    healthcheck:
      test:
        - CMD-SHELL
        - test -f /tmp/scheduler_alive && find /tmp/scheduler_alive -mmin -1 | grep -q scheduler_alive
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 512M

  execution_worker_paper:
    build: .
    container_name: polyflip_execution_worker_paper
    restart: always
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgresql+asyncpg://${DB_USER:-polyflip}:${DB_PASSWORD:-secret}@db/${DB_NAME:-polyflip}
      - EXECUTION_MODE=PAPER
    volumes:
      - ./polyflip:/app/polyflip
      - ./scripts:/app/scripts
    command: python -m polyflip.execution.worker
    deploy:
      resources:
        limits:
          memory: 512M

  execution_worker_live:
    build: .
    container_name: polyflip_execution_worker_live
    restart: always
    depends_on:
      - db
    profiles: ["live"]
    environment:
      - DATABASE_URL=postgresql+asyncpg://${DB_USER:-polyflip}:${DB_PASSWORD:-secret}@db/${DB_NAME:-polyflip}
      - POLYGON_PRIVATE_KEY=${POLYGON_PRIVATE_KEY}
      - POLYGON_ADDRESS=${POLYGON_ADDRESS}
      - EXECUTION_MODE=LIVE
    volumes:
      - ./polyflip:/app/polyflip
      - ./scripts:/app/scripts
    command: python -m polyflip.execution.worker
    deploy:
      resources:
        limits:
          memory: 512M

  execution_worker_shadow:
    build: .
    container_name: polyflip_execution_worker_shadow
    restart: always
    depends_on:
      - db
    profiles: ["shadow"]
    environment:
      - DATABASE_URL=postgresql+asyncpg://${DB_USER:-polyflip}:${DB_PASSWORD:-secret}@db/${DB_NAME:-polyflip}
      - EXECUTION_MODE=SHADOW
    volumes:
      - ./polyflip:/app/polyflip
      - ./scripts:/app/scripts
    command: python -m polyflip.execution.worker
    deploy:
      resources:
        limits:
          memory: 256M

volumes:
  polyflip_db_data:
