# Dashboard policy causal replay — run_20260913_10d

Дата выгрузки: `2026-09-12T18:35:16.655419+00:00`. Временной охват: последние 10 дней на момент выгрузки. Сервер использован только для read-only экспорта; replay выполнен локально.

## Проверки целостности

- Возможностей: **136895**, уникальных `opportunity_id`: **136895**.
- Все четыре варианта получили одну и ту же вселенную: `True`.
- Полностью пригодных строк для расчёта: **37124**. Неполные строки имеют `INSUFFICIENT_DATA`, они не превращены в нулевой PnL.
- Оба L1 ask/mid доступны только для **37804** строк; для BTC — **0** строк. Поэтому этот прогон не является оценкой BTC.
- Возраст ближайшей причинной котировки: медиана **29.5 с**, P90 **44.9 с**.

## Фиксированные варианты

| Вариант | BUY | Net PnL, USDC | Missing data |
|---|---:|---:|---:|
| CURRENT | 6616 | -695.63 | 99619 |
| STRICT | 315 | -38.17 | 99582 |
| FAVORITE_ONLY | 4306 | -1050.97 | 99594 |
| OUTSIDER_ONLY | 2310 | 355.34 | 99603 |


Комиссия: каноническая price-dependent модель (`fee_rate=0.07`, `exponent=1.0`), дополнительное проскальзывание 0.5%, бюджет 1 USDC. Пороговые значения заранее зафиксированы в коде; подбор по этому прогону не выполнялся.

## Интерпретация

`CURRENT` и `STRICT` отрицательны на этой выгрузке. `FAVORITE_ONLY` хуже `CURRENT`. `OUTSIDER_ONLY` положителен, но это сценарный результат на неполной вселенной: BTC отсутствует, почти 100 тысяч строк не имеют полного причинного набора, а котировки в основном старше 30 секунд. Поэтому это сигнал для отдельной проверки, а не доказательство прибыльности.

Положительный результат также нельзя переносить на live execution: replay использует observed L1 ask и не моделирует очередь maker, частичные исполнения и фактическую доступность объёма.

Полные строки replay хранятся вне Git как `policy_replay_opportunities.csv`; их SHA-256 зафиксирован в `manifest.json`. Повторить расчёт можно командой:

```text
python scripts/research/replay_dashboard_policy.py --input <export.csv> --output-dir <output-dir>
```
