# Dashboard policy causal replay — run_20260913_10d

Дата выгрузки: `2026-09-12T18:43:54.040669+00:00`. Временной охват: последние 10 дней на момент выгрузки. Сервер использован только для read-only экспорта; replay выполнен локально.

## Проверки целостности

- Возможностей: **136871**, уникальных `opportunity_id`: **136871**.
- Все четыре варианта получили одну и ту же вселенную: `True`.
- Полностью пригодных строк для расчёта: **37317**. Неполные строки имеют `INSUFFICIENT_DATA`, они не превращены в нулевой PnL.
- Оба L1 ask/mid доступны только для **37895** строк; для BTC — **0** строк. Поэтому этот прогон не является оценкой BTC.
- Возраст ближайшей причинной котировки: медиана **29.5 с**, P90 **44.9 с**.
- `final_outcome` не считается доступным по времени исторического снапшота: он ретроспективно backfill-ится. В ledger дата метки установлена на `market_end_at`; строка без причинно допустимого конца рынка остаётся `INSUFFICIENT_DATA`.

## Фиксированные варианты

| Вариант | BUY | Net PnL, USDC | Missing data |
|---|---:|---:|---:|
| CURRENT | 6657 | -666.98 | 99473 |
| STRICT | 319 | -33.72 | 99463 |
| FAVORITE_ONLY | 4322 | -1046.79 | 99470 |
| OUTSIDER_ONLY | 2335 | 379.81 | 99466 |


Комиссия: каноническая price-dependent модель (`fee_rate=0.07`, `exponent=1.0`), дополнительное проскальзывание 0.5%, бюджет 1 USDC. Пороговые значения заранее зафиксированы в коде; подбор по этому прогону не выполнялся.

## Интерпретация

`CURRENT` и `STRICT` отрицательны на этой выгрузке. `FAVORITE_ONLY` хуже `CURRENT`. `OUTSIDER_ONLY` положителен, но это сценарный результат на неполной вселенной: BTC отсутствует, около 99 тысяч строк не имеют полного причинного набора, а котировки в основном старше 30 секунд. Поэтому это сигнал для отдельной проверки, а не доказательство прибыльности.

Положительный результат также нельзя переносить на live execution: replay использует observed L1 ask и не моделирует очередь maker, частичные исполнения и фактическую доступность объёма.

Полные строки replay хранятся вне Git как `policy_replay_opportunities.csv`; их SHA-256 зафиксирован в `manifest.json`. Повторить экспорт можно через `scripts/research/policy_replay_ledger.sql`, а расчёт — командой:

```text
python scripts/research/replay_dashboard_policy.py --input <export.csv> --output-dir <output-dir>
```
