# reason_codes.yaml — FROZEN at Этап 1 (hash-locked with specs)
# Closed enum. Unknown reasons forbidden (enforced by schema-enum test).
# Suffix convention: _MISSING (data absent), _EXCLUDED (row dropped with cause),
# _FLAG (row kept with marker), _VIOLATION (rule breach under test).
version: 1.0.0
categories:
  MODEL:
    - MODEL_OK
    - MODEL_UNKNOWN_MODEL
    - MODEL_NO_MODEL
    - MODEL_MISSING_MODEL_ARTIFACT
    - MODEL_TARGET_CONFLICT
    - MODEL_NON_REPRODUCIBLE
    - MODEL_VERSION_MISMATCH
    - MODEL_REGIME_MISMATCH
  TARGET:
    - TARGET_OK
    - TARGET_UNKNOWN_EXCLUDED
    - TARGET_CONFLICT_EXCLUDED
    - TARGET_MISMATCH
    - TARGET_MID_FIFTY_EXCLUDED
  DECISION_TIME:
    - DECISION_TIME_OK
    - DECISION_TIME_MISSING_EXCLUDED
    - DECISION_TIME_PROXY_FLAG
    - DECISION_TIME_PROXY_SENSITIVITY
  QUOTE:
    - QUOTE_OK
    - QUOTE_ONE_SIDED_EXCLUDED
    - QUOTE_BAD_PRICE_EXCLUDED
    - QUOTE_SYNTHETIC_NO_FLAG
    - QUOTE_CROSSED_BOOK_EXCLUDED
    - QUOTE_CROSS_TOKEN_ARBITRAGE_FLAG
    - QUOTE_ASYNC_SIDES_EXCLUDED
  CANDLE:
    - CANDLE_OK
    - CANDLE_MISSING_FLAG
    - CANDLE_IMPUTED_CLOSE_FLAG
    - CANDLE_STALE_EXCLUDED
  SETTINGS:
    - SETTINGS_TIER_A
    - SETTINGS_TIER_B
    - SETTINGS_TIER_C
    - SETTINGS_UNKNOWN_EXCLUDED
  EXECUTION:
    - EXECUTION_NO_REQUEST
    - EXECUTION_NO_FILL
    - EXECUTION_PARTIAL_FILL_FLAG
    - EXECUTION_FILLED
    - EXECUTION_NO_TRADE
  OUTCOME:
    - OUTCOME_OK
    - OUTCOME_PENDING_EXCLUDED
    - OUTCOME_CONFLICT_EXCLUDED
    - OUTCOME_UNRESOLVED_48H_EXCLUDED
  LEAKAGE:
    - LEAKAGE_CLEAN
    - LEAKAGE_FUTURE_FEATURE_BLOCK
    - LEAKAGE_RISK
  COVERAGE:
    - COVERAGE_IN_SCOPE
    - COVERAGE_BELOW_GATE
    - COVERAGE_GAP_FOLD_EXCLUDED
